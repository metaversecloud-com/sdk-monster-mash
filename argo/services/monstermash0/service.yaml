apiVersion: v1
kind: Service
metadata:
  name: monstermash0
  labels:
    app.kubernetes.io/name: monstermash0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: monstermash0
  ports:
    - name: http
      port: 80
      targetPort: http
